VITE_API_URL=http://localhost:4000/api/v1
# Google Maps (Settings location picker). Requires billing + Maps JavaScript API + Places API.
VITE_GOOGLE_MAPS_API_KEY=

# No frontend QZ secrets — certificate/signing is served by the backend when QZ_CERTIFICATE + QZ_PRIVATE_KEY are set.
